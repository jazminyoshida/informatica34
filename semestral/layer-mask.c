#include <stdio.h>
int main(void){
    //How do layer masks work in Photoshop? layer mask works to edit an image without loosing the original image, we can use layer mask to hide parts of an image or show more.
    int 0, 255;
    printf("Enter two integers: ");
    scanf("%d %d", &0, &255);

    if(0 == 0) {
        printf("Result: %d = %d",Hidden ,Heidden);
    }

    else if (255> 0) {
        printf("Result: %d > %d",255 ,0 );
    }

    else {
        printf("Result: %d < %d",0, 255);
    }

}

